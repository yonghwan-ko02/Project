[pytest]
# Pytest configuration file

# Custom markers
markers =
    slow: Slow tests (real API calls, takes time)
    unit: Unit tests
    integration: Integration tests

# Test paths
testpaths = tests

# Output options
addopts = -v --tb=short

# Python path
pythonpath = .
